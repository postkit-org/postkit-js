---
import type { KeyTakeawayProps } from '@postkit/react';
import { KeyTakeaway } from '../lib/react-bridges';
type Props = KeyTakeawayProps;
const props = Astro.props;
---
<KeyTakeaway {...props}><slot /></KeyTakeaway>
