---
import type { FileTreeProps } from '@postkit/react';
import { PostkitAstroFileTree } from '../lib/react-bridges';
type Props = FileTreeProps;
const props = Astro.props;
---
<PostkitAstroFileTree {...props} />
