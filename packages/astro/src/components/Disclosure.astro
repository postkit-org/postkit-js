---
import type { DisclosureProps } from '@postkit/react';
import { Disclosure } from '../lib/react-bridges';
type Props = DisclosureProps;
const props = Astro.props;
---
<Disclosure {...props}><slot /></Disclosure>
