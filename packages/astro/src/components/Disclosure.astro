---
import type { PostkitDisclosureProps } from '@postkit/react';
import { PostkitAstroDisclosure } from '../lib/react-bridges';
type Props = PostkitDisclosureProps;
const props = Astro.props;
---
<PostkitAstroDisclosure {...props}><slot /></PostkitAstroDisclosure>
