---
import type { CallToActionProps } from '@postkit/react';
import { CallToAction } from '../lib/react-bridges';

type Props = CallToActionProps;
const props = Astro.props;
---

<CallToAction {...props} />
