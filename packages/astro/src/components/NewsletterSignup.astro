---
import type { NewsletterSignupProps } from '@postkit/react';
import { PostkitAstroNewsletterSignup } from '../lib/react-bridges';

type Props = NewsletterSignupProps;
const props = Astro.props;
---

<PostkitAstroNewsletterSignup {...props} client:visible />
