---
import type { ShareActionsProps } from '@postkit/react';
import { ShareActions } from '../lib/react-bridges';

type Props = ShareActionsProps;
const props = Astro.props;
---

<ShareActions {...props} client:visible />
