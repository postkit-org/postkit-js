---
import type { AuthorCardProps } from '@postkit/react';
import { PostkitAstroAuthorCard } from '../lib/react-bridges';

type Props = AuthorCardProps;
const props = Astro.props;
---

<PostkitAstroAuthorCard {...props} />
