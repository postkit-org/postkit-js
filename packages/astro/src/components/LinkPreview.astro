---
import type { LinkPreviewProps } from '@postkit/react';
import { PostkitAstroLinkPreview } from '../lib/react-bridges';

type Props = LinkPreviewProps;
const props = Astro.props;
---

<PostkitAstroLinkPreview {...props} client:visible />
