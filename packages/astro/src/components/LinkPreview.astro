---
import type { LinkPreviewProps } from '@postkit/react';
import { LinkPreview } from '../lib/react-bridges';

type Props = LinkPreviewProps;
const props = Astro.props;
---

<LinkPreview {...props} client:visible />
