---
import type { CarouselProps } from '@postkit/react';
import { Carousel } from '../lib/react-bridges';

type Props = CarouselProps;
const props = Astro.props;
---

<Carousel {...props} client:visible>
  <slot />
</Carousel>
