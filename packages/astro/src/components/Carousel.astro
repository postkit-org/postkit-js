---
import type { CarouselProps } from '@postkit/react';
import { PostkitAstroCarousel } from '../lib/react-bridges';

type Props = CarouselProps;
const props = Astro.props;
---

<PostkitAstroCarousel {...props} client:visible>
  <slot />
</PostkitAstroCarousel>
