---
import type { SocialPostProps } from '@postkit/react';
import { SocialPost } from '../lib/react-bridges';

type Props = SocialPostProps;
const props = Astro.props;
---

<SocialPost {...props} client:visible />
