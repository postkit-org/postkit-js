---
import type { AudienceBoundaryProps } from '@postkit/react';
import { AudienceBoundary } from '../lib/react-bridges';
type Props = AudienceBoundaryProps;
const props = Astro.props;
---
<AudienceBoundary {...props}><slot /></AudienceBoundary>
